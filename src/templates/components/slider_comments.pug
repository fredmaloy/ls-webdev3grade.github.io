script(type="text/x-template")#commentsId
    .comments__slider
        .section__comments-headline
            h2.headline_second-level.comments__headline Что обо мне говорят
            .slider__comments-buttons
                button.slider__comments-button__circle(@click="previous()")
                    svg.slider__comments-circle__svg(viewBox='0 0 100 100', preserveAspectRatio='none')
                        circle.circle__comment(cx='50', cy='50', r='40')
                    .slider__button-arrow.white__style.left
                        .slider__button-arrow__size    
                            +icon(`arrow-down`, '.slider__button-arrow__left-svg')
                button.slider__comments-button__circle(@click="next()")
                    svg.slider__comments-circle__svg(viewBox='0 0 100 100', preserveAspectRatio='none')
                        circle.circle__comment(cx='50', cy='50', r='40')
                    .slider__button-arrow.white__style.right
                        .slider__button-arrow__size
                            +icon(`arrow-down`, '.slider__button-arrow__right-svg')        
        .comments__content
            flickity(ref="flickity" :options="flickityOptions").comments__content-list
                .comments__content-elem(v-for="comment in comments")
                    .quote
                        +icon(`quote`, '.quote-svg')
                    .comments__content-elem__feedback
                        .comments__content-elem__feedback-text 
                            p {{comment.text}}
                        .comments__content-elem__feedback-user-card
                            .feedback-user-card-image       
                                img(:src="comment['author-pic']").user-pic.comment
                            .feedback-user-card-info
                                .feedback-user-card-name {{comment['author-name']}}
                                .feedback-user-card-position {{comment['author-occ']}}