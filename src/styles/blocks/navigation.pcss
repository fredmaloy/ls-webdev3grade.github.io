.header__navigation {
    @include tablets {
        display: none;
    }
}

.footer__navigation {
    display: flex;

    @include tablets {
      align-items: flex-start;
      justify-content: flex-start;
    }
}

.navigation__list {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;

     @include tablets {
      justify-content: flex-start;
    }
}

.navigation__list-elem {
    margin-right: 50px;
    
    &:hover {
        color: #9299a1;
    }

    &:last-child {
        margin-right: 0;
    }

    @include tablets {
      padding-bottom: 10px
    }
}